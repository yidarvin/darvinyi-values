{
  "name": "darvinyi-values",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --include=dev --prefix web && npm run build --prefix web",
    "start": "node api/scripts/migrate.js && node api/scripts/seed-values.js && node index.js",
    "dev": "node index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}
